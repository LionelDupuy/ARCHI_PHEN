# -*- mode: python -*-
a = Analysis(['MainFrame.py'],
             pathex=['D:\\LIONEL\\PROGRAMMING\\CODE\\CORE\\ARCHI_SUITE\\ARCHI_PHEN\\V5'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='MainFrame.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='MainFrame')
